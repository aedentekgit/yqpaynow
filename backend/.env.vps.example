# ==============================================
# VPS UPLOAD CONFIGURATION
# ==============================================

# VPS Upload Path - Where files will be saved on the VPS
VPS_UPLOAD_PATH=/var/www/html/uploads

# VPS Base URL - Public URL to access uploaded files
# Use your domain if you have one, otherwise use IP
VPS_BASE_URL=https://yqpaynow.com
# OR
# VPS_BASE_URL=http://147.79.68.136

# ==============================================
# IMPORTANT NOTES:
# ==============================================
# 1. Make sure /var/www/html/uploads directory exists on your VPS
# 2. Set proper permissions: sudo chmod -R 755 /var/www/html/uploads
# 3. Set proper ownership: sudo chown -R www-data:www-data /var/www/html/uploads
#    (or use your Node.js process user instead of www-data)
# 4. Ensure your web server (Apache/Nginx) is configured to serve /uploads/
# 5. For HTTPS, make sure SSL certificate is properly configured
